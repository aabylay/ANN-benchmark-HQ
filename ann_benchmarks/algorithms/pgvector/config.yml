float:
  any:
  - base_args:
    - '@metric'
    constructor: PGVector
    disabled: false
    docker_tag: ann-benchmarks-pgvector
    module: ann_benchmarks.algorithms.pgvector
    name: pgvector
    run_groups:
      HNSW:
        args:
          M:
          - 16
          efConstruction:
          - 128
        query_args:
        - - 100
          - 200
          - 300
          - 400
          - 500
          - 600
          - 700
          - 800
          - 900
          - 1000
