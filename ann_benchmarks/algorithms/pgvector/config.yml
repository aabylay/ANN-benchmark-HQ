float:
  any:
  - base_args:
    - '@metric'
    constructor: PGVector
    disabled: false
    docker_tag: ann-benchmarks-pgvector
    module: ann_benchmarks.algorithms.pgvector
    name: pgvector
    run_groups:
      HNSW:
        args:
          M:
          - 15
          efConstruction:
          - 75
        query_args:
        - - 100
          - 200
          - 500
          - 1000
